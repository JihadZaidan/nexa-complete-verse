import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // primary option for styled-components //
  compiler: {
    styledComponents: true,
  },

  // configuration other option to added //
};

export default nextConfig;
